package com.horasan.cs_dojo.arrays;

public class DynamicArray {
	
	private int capacity;
	private int length;
	private String items[];
	
	public DynamicArray() {
		capacity = 1;
		length = 0;
		items = new String[1];
	}
	
	// get(index)
	// push(newItem) add to the end
	// pop() remove last item
	// replace(index, newValue) 
	// delete(index)
	// getLength()
	// insert(index, newValue)
	
}
